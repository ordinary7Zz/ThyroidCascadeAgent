1、目前已经用finetune训练了多个分割模型，将这些模型的输出（包括dinov3），作为radiomics的输入，输出多个分类判断，用分类的置信度和radiomics输出的特征信息（也可以结合dinov3和其他finetune的分类模型的分类输出辅助判断），来让agent评估分割输出是否可信：如果差异较大，需要让agent挑选出更可信的分割输出；
注意：需要先拿GT的分割结果训练一个radiomics模型，观察性能怎么样，然后把各个分割模型的结果通过这个模型来衡量不确定性

2、根据筛选完的分割结果，让agent再次评估radiomics和其他分类模型输出的结果，输出选择后的分类结果